def validate_doi_identifiers(doiIdentifiers):
    """
    This function validates a list of DOI identifiers represented as dictionaries.

    Parameters:
    __________
    doiIdentifiers: list[dict]
        The DOI identifiers that need to be validated

    Raises:
    ______
    ValidationError
        If any identifier is found to be invalid or if doiIdentifiers is not a list of dictionaries.
    """
    return
#"doiIdentifiers": [
 #     {
  #      "identifier": "string",
   #     "id": 0,
    #    "url": "string"
     # }
   # ],


def validate_pubmed_identifiers(pubmedIdentifiers):
    """
    This function validates a list of PubMed identifiers represented as dictionaries.

    Parameters:
    __________
    pubmedIdentifiers: list[dict]
        The PubMed identifiers that need to be validated

    Raises:
    ______
    ValidationError
        If any identifier is found to be invalid or if pubMed Identifiers is not a list of dictionaries.
    """
    #"pubmedIdentifiers": [
    #  {
    #    "identifier": "string",
    #    "id": 0,
      #  "url": "string",
     #   "referenceHtml": "string"
      #}
    return